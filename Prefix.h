#ifndef PREFIX
#define PREFIX
#include <string>
#include<vector>

class Prefix{
    public:
    std::string prefix(std::string input,std::vector<std::string> list, int firstint);
};


#endif